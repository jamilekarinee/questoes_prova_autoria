// capitalizarTitulos


const capitalizarTitulos = (arrayDeStrings) => arrayDeStrings.map(string => string.toUpperCase());

console.log(capitalizarTitulos(["o pequeno principe","dom casmurro","a metamorfose"]));// length[i] 
//.split(\)

// Tentei realizar esta questões de muitas formas, até com 
// return livros[0].[0].toUpperCase(); 
// mas não consegui. Tem como o senhor postar as respostas depois, por gentileza? 

